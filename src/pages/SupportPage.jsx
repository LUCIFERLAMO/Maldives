import React from 'react';

const SupportPage = () => {
    return (
        <div className="min-h-screen bg-slate-50">
            {/* Content cleared as requested. Navbar remains via App layout. */}
        </div>
    );
};

export default SupportPage;
